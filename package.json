{
  "x-comment": [
    "1. 工作区根 package 配置。",
    "2. 作用：声明 monorepo 的 workspaces、共享脚本和统一开发依赖。",
    "3. 维护要点：新增包或升级基础工具链时，应优先在这里收口。"
  ],
  "name": "statekit-workspace",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev:docs": "npm run dev --workspace @statekit/docs",
    "dev:example": "npm run dev --workspace @statekit/example-vite-vue-admin",
    "build": "npm run build --workspace @statekit-vue/shared && npm run build --workspace @statekit-vue/vue && npm run build --workspace @statekit/docs && npm run build --workspace @statekit/example-vite-vue-admin",
    "typecheck": "npm run typecheck --workspace @statekit-vue/shared && npm run typecheck --workspace @statekit-vue/vue && npm run typecheck --workspace @statekit/docs && npm run typecheck --workspace @statekit/example-vite-vue-admin",
    "pack:check": "npm pack --workspace @statekit-vue/shared --json --dry-run && npm pack --workspace @statekit-vue/vue --json --dry-run",
    "smoke:install": "node ./scripts/smoke-install.mjs",
    "lint:boundaries": "node ./scripts/check-boundaries.mjs",
    "test:ui": "playwright test",
    "test:unit": "vitest run",
    "verify:fast": "npm run lint:boundaries && npm run typecheck && npm run test:unit && npm run build",
    "verify:ui": "npm run test:ui",
    "verify:release": "npm run lint:boundaries && npm run typecheck && npm run build && npm run test:unit && npm run test:ui && npm run pack:check && npm run smoke:install",
    "verify": "npm run verify:release",
    "review:bundle": "node ./scripts/agent-review-bundle.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^20.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^26.1.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^2.1.9",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0",
    "vue-tsc": "^2.0.0"
  }
}
